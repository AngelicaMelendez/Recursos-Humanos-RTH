{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:migrate": "npx sequelize-cli db:migrate --config config/config.js --migrations-path migrations --models-path models",
    "db:migrate:undo": "npx sequelize-cli db:migrate:undo --config config/config.js --migrations-path migrations --models-path models",
    "db:migrate:undo:all": "npx sequelize-cli db:migrate:undo:all --config config/config.js --migrations-path migrations --models-path models",
    "db:clear-data": "node scripts/clearDatabaseData.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "mysql2": "^3.22.3",
    "pdfkit": "^0.18.0",
    "sequelize": "^6.37.8",
    "vue3-org-chart": "^0.2.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
