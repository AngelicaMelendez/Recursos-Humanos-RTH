'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "vacantes".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('vacantes', null, {});
  },
};
