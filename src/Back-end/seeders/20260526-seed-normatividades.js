'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "normatividades".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('normatividades', null, {});
  },
};
