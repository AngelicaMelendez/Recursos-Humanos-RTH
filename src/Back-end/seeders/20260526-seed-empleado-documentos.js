'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "empleado_documentos".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('empleado_documentos', null, {});
  },
};
