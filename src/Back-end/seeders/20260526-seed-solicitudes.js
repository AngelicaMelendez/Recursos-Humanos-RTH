'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "solicitudes".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('solicitudes', null, {});
  },
};
