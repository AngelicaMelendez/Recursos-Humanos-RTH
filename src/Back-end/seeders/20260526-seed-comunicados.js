'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "comunicados".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('comunicados', null, {});
  },
};
