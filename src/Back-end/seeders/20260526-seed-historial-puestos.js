'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "historial_puestos".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('historial_puestos', null, {});
  },
};
