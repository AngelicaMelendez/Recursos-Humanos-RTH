'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // TODO: Añade datos para "incidencias".
  },

  async down(queryInterface, Sequelize) {
    // await queryInterface.bulkDelete('incidencias', null, {});
  },
};
